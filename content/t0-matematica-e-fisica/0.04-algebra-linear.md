# 0.04 algebra linear

@card bWRKWnVAR09jRg
$\mathbf{u} \cdot \mathbf{v} \ (\text{Produto Escalar})$
@@
$u_1v_1 + u_2v_2 + \dots + u_nv_n$

@card bzl3Sl9rTS5xUQ
Potência Elétrica e Produto Escalar ($P = \mathbf{F} \cdot \mathbf{v}$)
@@
Potência só é positiva quando Força e Deslocamento estão alinhados. Componentes perpendiculares não realizam trabalho.

@card aXw8ckgmK1ZyOg
$\mathbf{u} \cdot \mathbf{v} \ (\text{Ângulo entre Vetores})$
@@
$||\mathbf{u}|| ||\mathbf{v}|| \cos(\theta)$

@card aXg2O0swX3F5Sw
$$\mathbf{u} \perp \mathbf{v} \iff \dots \ (\text{Condição de Ortogonalidade})$$
@@
$\mathbf{u} \cdot \mathbf{v} = 0$

@card ci1OTnNBd2N3VA
$S \text{ é conjunto ortonormal } \iff \dots$
@@
$\mathbf{v}_i \cdot \mathbf{v}_j = 0 \ (i \neq j) \land ||\mathbf{v}_i|| = 1$

@card LyQoWUhnREh3RA
Gram-Schmidt — procedimento
@@
Transforma base qualquer em base ortonormal: 1) $e_1 = v_1/\|v_1\|$. 2) Subtrair projeções nos anteriores. 3) Normalizar. Resultado: base ON.

@card YmZ2a1F0TmVYRg
$|\mathbf{u} \cdot \mathbf{v}| \ (\text{Desigualdade de Cauchy-Schwarz})$
@@
$\le ||\mathbf{u}|| ||\mathbf{v}||$

@card bm1SW1Z7Lk80ZQ
$$||\mathbf{u} + \mathbf{v}|| \ (\text{Desigualdade Triangular})$$
@@
$\le ||\mathbf{u}|| + ||\mathbf{v}||$

@card altWIUM0KWQuNw
$d(\mathbf{u}, \mathbf{v}) \ (\text{Distância Vetorial})$
@@
$||\mathbf{u} - \mathbf{v}||$

@card RE9ffD8tW3c0ZQ
$$T \text{ é Transformação Ortogonal } \iff \dots$$
@@
$||T(\mathbf{u})|| = ||\mathbf{u}|| \ (\text{Preserva distâncias})$

@card bnBQWT95dXhWVA
$\mathbf{u} \ (\text{Vetor Unitário na Direção de } \mathbf{v})$
@@
$\frac{1}{||\mathbf{v}||}\mathbf{v}$

@card bXs-TipvZUpKUg
$(A+B)_{ij} \ (\text{Soma de Matrizes})$
@@
$A_{ij} + B_{ij}$

@card bDdgcSUjOCR6bA
$(A+B)^T \ (\text{Transposta da Soma})$
@@
$A^T + B^T$

@card RkxFTXB2bj5bUg
$(A^T)^{-1} \ (\text{Inversa da Transposta})$
@@
$(A^{-1})^T$

@card QjUqWm9vKEs6bg
$(A^T)_{ij} \ (\text{Transposta de Matriz})$
@@
$A_{ji}$

@card TzBzTzMoOWx1Pg
$(AB)^T \ (\text{Transposta do Produto})$
@@
$B^T A^T$

@card ZT9HYkpZN1AvRA
$(kA)^T \ (\text{Transposta de Matriz por Escalar})$
@@
$kA^T$

@card ajdCajYqa3lsWw
$\det(A^T) \ (\text{Determinante da Transposta})$
@@
$\det(A)$

@card Rnh3PFNZdykkRg
$\text{adj}(A) \ (\text{Matriz Adjunta})$
@@
$C^T \ (\text{Transposta da matriz de cofatores})$

@card eV1wTkxUKSlzLA
$\text{rank}(A^T) \ (\text{Posto da Matriz Transposta})$
@@
$\text{rank}(A)$

@card RWhiXl9eN11ZPQ
$\text{tr}(A^T) \ (\text{Traço da Matriz Transposta})$
@@
$\text{tr}(A)$

@card Y2N7dEd2P0ZqcQ
$\text{tr}(A) \ (\text{Traço da Matriz})$
@@
$a_{11} + a_{22} + \dots + a_{nn}$

@card aTpaSDhlR3lCUQ
$\text{tr}(AB) \ (\text{Comutatividade do Traço})$
@@
$\text{tr}(BA)$

@card SjYxPihyZ3c_ag
$\mathbf{u}^T \mathbf{v} \ (\text{Produto Interno em Matrizes})$
@@
$\text{tr}(\mathbf{u}\mathbf{v}^T)$

@card R2o3REUvI2dOVA
$(A^T)^T \ (\text{Dupla Transposição})$
@@
$A$

@card dz9mMURJZE1Uag
$(A^n)^{-1} \ (\text{Inversa da Potência})$
@@
$(A^{-1})^n$

@card aCxPdW1NJXZJSA
$(AB)^{-1} \ (\text{Inversa do Produto})$
@@
$B^{-1} A^{-1}$

@card c0dFfWdiKmwwXg
$(kA)^{-1} \ (\text{Inversa de Matriz por Escalar})$
@@
$\frac{1}{k}A^{-1}$

@card ZCMzWDkhPSYocQ
$[T^{-1}] \ (\text{Inversa de Transformação Linear})$
@@
$[T]^{-1}$

@card ZWFbLFMreGFQQg
$\det(A^{-1}) \ (\text{Determinante da Inversa})$
@@
$\frac{1}{\det(A)}$

@card azk2d2NpVyZmOQ
$A^{-1} \ (\text{Inversa via Matriz Adjunta})$
@@
$\frac{1}{\det(A)}\text{adj}(A)$

@card VlEoaWsrLSxQTg
SVD — aplicações práticas
@@
1) Compressão de imagem (truncar $\Sigma$). 2) PCA (redução de dimensionalidade). 3) Pseudo-inversa ($A^+ = V \Sigma^+ U^T$). 4) Resolução de mínimos quadrados.

@card QSh5fEklS3BHfg
$A\mathbf{x} = \mathbf{b} \text{ Consistente} \iff \mathbf{b} \dots \ (\text{Solução de Sistema Linear})$
@@
$\in \text{col}(A) \ (\text{Combinação linear das colunas})$

@card QyxHJXg-S2dvNg
$$\begin{aligned}\mathbf{x}_p \ (\text{Sol. Particular}) &\implies \\ &\land \mathbf{x}_h \ (\text{Sol. Homogênea}) \\ &\implies \implies \mathbf{x}_{geral} \implies \\ &= \dots \ (\text{Solução Geral de Sistema Linear})\end{aligned}$$
@@
$\mathbf{x}_p + \mathbf{x}_h$

@card SVNGK0t8OExAVw
$[T_2 \circ T_1] \ (\text{Composição de Transformações Lineares})$
@@
$[T_2][T_1]$

@card eCs4OXgxLTs4Pw
$\det(A) \ (\text{Expansão de Cofatores linha } i)$
@@
$\sum_j a_{ij}C_{ij}$

@card aCtYbl9SWywpUA
$\ker(T) \ (\text{Núcleo de Transformação Linear / Espaço Nulo})$
@@
$\{ \mathbf{x} \mid T(\mathbf{x}) = \mathbf{0} \}$

@card aExUWihjfW1DMg
$\text{rank}(A) \ (\text{Posto de Matriz})$
@@
$\text{dim}(\text{row}(A)) = \text{dim}(\text{col}(A))$

@card aEtWI001IzB9eQ
$\text{ran}(T) \ (\text{Imagem de Transformação Linear})$
@@
$\{ \mathbf{y} \mid \exists \mathbf{x}, T(\mathbf{x}) = \mathbf{y} \}$

@card UEhONmA_azpdUA
$\text{row}(A)^\perp \ (\text{Complemento Ortogonal do Espaço Linha})$
@@
$\text{null}(A)$

@card cCFDfVZoUH0yLQ
$A \text{ Ortogonal } \implies \det(A)$
@@
$\pm 1$

@card RFd6LkZ2dnAsQg
$$\lambda \text{ é Autovalor de } A \iff \dots \ (\text{Definição de Autovalores e Autovetores})$$
@@
$A\mathbf{x} = \lambda \mathbf{x} \ (\exists \mathbf{x} \neq \mathbf{0})$

@card cyk5ZjdqSC07RQ
$p(\lambda) \ (\text{Polinômio Característico})$
@@
$\det(\lambda I - A) \lor \det(A - \lambda I)$

@card cTlTY1h3dVB2bw
$S \text{ é Base de } V \iff \dots$
@@
$S \text{ é LI } \land \text{span}(S) = V$

@card eH51WlVyYmZHPg
$T \text{ é Transformação Linear } \iff \dots$
@@
$T(c\mathbf{u}) = cT(\mathbf{u}) \land T(\mathbf{u} +\mathbf{v}) = T(\mathbf{u}) + T(\mathbf{v})$

@card US5rfHpXaDFKWQ
$z^n \ (\text{Teorema de De Moivre para Potência Complexa})$
@@
$r^n e^{in\theta} = r^n(\cos(n\theta) + i\sin(n\theta))$

@card QiopYmBfQ1lkbg
$z_1z_2 \ (\text{Multiplicação de Complexos na Forma Polar})$
@@
$r_1r_2 e^{i(\theta_1+\theta_2)}$

@card azU7ZFFETC8pMg
$$\text{dim}(W) + \text{dim}(W^\perp) \ (\text{Soma de Dimensões em } \mathbb{R}^n)$$
@@
$n$

@card QT1tKklmSj9DWg
$$\text{rank}(A) + \text{nullity}(A) \ (\text{Teorema do Posto-Nulidade})$$
@@
$n \ (\text{Nº de colunas})$

@card SFNDQnsvaEcveg
$$A \text{ é Antissimétrica (Skew-Symmetric)} \iff \dots$$
@@
$A^T = -A$

@card aTV8JkhraWImSw
$$A \text{ é Invertível} \iff \dots \ (\text{Matriz Não-Singular})$$
@@
$\det(A) \neq 0$

@card SlZ3WWFNcTh5bw
$c_1\mathbf{v}_1 + \dots + c_n\mathbf{v}_n = 0 \implies \implies S \text{ é LI } \implies \iff \dots \ (\text{Independência Linear})$
@@
$c_1 = \dots = c_n = 0$

@card elhpUEcvWFJGSQ
$T \ (\text{Projeção Ortogonal no eixo } x) \implies T(x,y)$
@@
$(x, 0)$

@card SXtSQFk-fEtXLw
$T \ (\text{Projeção Ortogonal no plano } xy) \implies T(x,y,z)$
@@
$(x, y, 0)$

@card aS8vPz80Xm10fg
$T \ (\text{Reflexão na reta } y =x) \implies T(x,y)$
@@
$(y, x)$

@card QVVSSDMpcHI2Ow
$T \ (\text{Reflexão no eixo } x) \implies T(x,y)$
@@
$(x, -y)$

@card bVRbXTBVaVg5Ug
$T \ (\text{Reflexão no eixo } y) \implies T(x,y)$
@@
$(-x, y)$

@card c1h2PmwlQHMzLA
$$T:\mathbb{R}^n \to \mathbb{R}^m \text{ é Sobrejetora (Onto)} \iff \dots$$
@@
$\text{col}(A) = \mathbb{R}^m$

@card eWVKUkl8Znlufg
$z = a +ib \implies |z| \ (\text{Módulo do Número Complexo})$
@@
$\sqrt{a^2+b^2}$

@card SGRwdmEyLWIvXw
Como a análise de malhas é formulada matricialmente
@@
$$\begin{aligned}\text{Sistema } Z \cdot \mathbf{I} \\ &= \mathbf{V}\text{, onde } Z \text{ é a matriz de impedâncias, } \mathbf{I} \text{ as correntes de malha e } \mathbf{V} \text{ as fontes.}\end{aligned}$$

@card S1lpcUh-WkQ_KA
Condição de controlabilidade de um sistema (Kalman)
@@
O sistema é controlável se a matriz $[B\ AB\ A^2B\ \dots\ A^{n-1}B]$ tem posto completo ($n$).

@card cjhEQXxacDB3dw
O que $\det(Z) = 0$ indica na matriz de impedâncias
@@
A rede é singular (dependência linear entre malhas). Pode indicar curto-circuito ou circuito impossível.

@card T31ocXxoa3opUw
O que representa a matriz $A$ no espaço de estados
@@
A dinâmica interna do sistema. Seus autovalores são os polos do sistema (definem estabilidade e resposta).

@card RlNeMWVBP11Gbg
O que um Autovalor ($\lambda$) Real Negativo significa num sistema dinâmico/circuito
@@
Estabilidade Absoluta (Decaimento Exponencial). O sistema dissipa a energia.

@card d2oudHZqLn0m
O que um Autovalor ($\lambda$) Real Positivo significa num sistema dinâmico/circuito
@@
Instabilidade (Crescimento Exponencial). O sistema explode e satura (ex: microfonia).

@card aCh8OV5VVVZ4bw
Número de condição de uma matriz e por que importa
@@
$$\begin{aligned}\text{Mede a sensibilidade da solução a erros numéricos. } \kappa(A) \text{ grande } \\ &\Rightarrow \text{ circuito/sistema mal condicionado (resultado instável numericamente).}\end{aligned}$$

@card RFYtJD9NTWlzdQ
Para aplicar a Superposição, como "desligamos" as fontes
@@
Fontes de Tensão  $\implies \text{Curto-Circuito (Fio Liso). Fontes de Corrente } \implies$ Circuito Aberto.

@card aShJKklKZmFXJA
Princípio da Superposição em Sistemas Lineares Elétricos
@@
A resposta total (Tensão/Corrente) é a soma das respostas individuais de cada fonte operando sozinha no circuito.

@card bVpvbUhOOGU2Yw
Representação em espaço de estados de um sistema LTI
@@
$\dot{\mathbf{x}} = A\mathbf{x} + B\mathbf{u}\text{, } \mathbf{y} = C\mathbf{x} + D\mathbf{u}$

@card S3M3QnBKSXR8bw
Sistema Linear Impossível ($\det(A) = 0$) aplicado a Circuitos
@@
Significa que o circuito está mal definido (ex: duas fontes de tensão diferentes em paralelo, curto-circuito impossível).

@card eHs2KGgvTHgyUA
SVD — interpretação geométrica
@@
Qualquer transformação linear = rotação ($V^T$) → escala ($\Sigma$) → rotação ($U$). Os valores singulares são os fatores de escala ao longo dos eixos principais.

@card a1gmNmNYN1Jmfg
$A \text{ é Simétrica} \iff \dots$
@@
$A^T = A$

@card Tn5QbTFyLkNxKA
$C_{ij} \ (\text{Cofator da Matriz})$
@@
$(-1)^{i+j}M_{ij}$

@card al5zZl5dLTtQYA
$\det(AB) \ (\text{Determinante do Produto})$
@@
$\det(A)\det(B)$

@card Y2hAaWx0UzFWdA
$\det(kA) \ (A_{n \times n}) \ (\text{Determinante de Matriz por Escalar})$
@@
$k^n \det(A)$

@card TF92WlsjTkktOg
$|\det(A)| \ (A_{2 \times 2}) \ (\text{Interpretação Geométrica do Determinante 2D})$
@@
Área do Paralelogramo

@card YlchKHpGZDZFcw
$|\det(A)| \ (A_{3 \times 3}) \ (\text{Interpretação Geométrica do Determinante 3D})$
@@
Volume do Paralelepípedo

@card bE41Pzp8LnQwPQ
$x_i \ (\text{Regra de Cramer})$
@@
$\frac{\det(A_i)}{\det(A)}$

@card Yyl9RGpfU19iTg
$z^{-1} \ (\text{Inverso de Número Complexo})$
@@
$\frac{\bar{z}}{|z|^2}$

@card bXhPdyRTWCM8Xw
$\frac{z_1}{z_2} \ (\text{Divisão de Complexos na Forma Polar})$
@@
$\frac{r_1}{r_2} e^{i(\theta_1-\theta_2)}$

@card eHVtcmBgTDFRbA
$e^{i\pi} + 1 \ (\text{Identidade de Euler})$
@@
$0$

@card QlMoKi50OEJwQA
$\mathbf{u} \times \mathbf{v} \ (\text{Anticomutatividade do Produto Vetorial})$
@@
$-\mathbf{v} \times \mathbf{u}$

@card ckpAdE1Na0N3ew
Conexão entre a Matriz de Estados de um circuito e sua Função de Transferência $G(s)$
@@
Os Polos do denominador de $G(s)$ são exatamente os Autovalores da Matriz de Estados do Sistema.

@card QUAucTo_TmBgLg
O que os Autovalores Complexos ($\lambda = \alpha \pm \beta i$) indicam no circuito
@@
Induzem oscilação. Se a parte real $\alpha  \lt  0$ (Estável e oscila decaindo). Se $\alpha  \gt  0$ (Instável, oscila crescendo).

@card SUpjS1o4YUcjYQ
$[R_\theta] \ (\text{Matriz de Rotação 2D})$
@@
$\begin{bmatrix} \cos\theta & -\sin\theta \\ \sin\theta & \cos\theta \end{bmatrix}$

@card YU1qYUR0aUUpWg
Projeção ortogonal de $\vec{b}$ sobre $\vec{a}$
@@
$\text{proj}_{\vec{a}} \vec{b} = \frac{\vec{a} \cdot \vec{b}}{\vec{a} \cdot \vec{a}} \vec{a}$. Base de mínimos quadrados e Gram-Schmidt.

@card cihtdV5xN2VTdg
$(S^\perp)^\perp \ (\text{Complemento do Complemento Ortogonal, Se } S \text{ é subespaço})$
@@
$S$

@card Sz8mVzZXPUN-XQ
$\text{col}(A)^\perp \ (\text{Complemento Ortogonal do Espaço Coluna})$
@@
$\text{null}(A^T)$

@card anh3UX1JZHpmXw
$A \text{ é Matriz Ortogonal } \iff \dots$
@@
$A^T = A^{-1}$

@card RThhNVNrUy93eg
$S^\perp \ (\text{Complemento Ortogonal})$
@@
$\{ \mathbf{v} \in \mathbb{R}^n \mid \mathbf{v} \cdot \mathbf{w} = 0\}$, $\forall \mathbf{w} \in S$

@card UDZ8VGhRKHpjVw
$T \text{ é Injetora (One-to-One)} \iff \dots$
@@
$\ker(T) = \{\mathbf{0}\}$

@card Lkd7ZVVMfT5NWg
SVD — Decomposição em Valores Singulares
@@
$A = U \Sigma V^T$. $U$: vetores singulares esquerdos (ortonormais), $\Sigma$: valores singulares (diagonal), $V^T$: vetores singulares direitos. Funciona para QUALQUER matriz.

@card S11oWWB5eXh0Rw
Parâmetros Z (impedância) de uma rede de dois portos
@@
$\begin{bmatrix} V_1 \\ V_2 \end{bmatrix} = \begin{bmatrix} Z_{11} & Z_{12} \\ Z_{21} & Z_{22} \end{bmatrix} \begin{bmatrix} I_1 \\ I_2 \end{bmatrix}$

@card c2ohJFtPazhEfg
$\mathbf{u}\mathbf{v}^T \ (\text{Matrizes de Tamanhos Iguais})$
@@
Produto Externo (Gera Matriz)

@card MVtOLU5-flgk
A DFT (Transformada Discreta de Fourier) é uma operação matricial
@@
Cada frequência $X[k]$ é o produto escalar do sinal $x[n]$ com um vetor base $e^{-j2\pi kn/N}$. A FFT é a versão eficiente.

@card Si5IKlozeE8rew
A força magnética ($F = q\mathbf{v} \times \mathbf{B}$) é um Produto Vetorial e não Escalar
@@
Porque a Força é sempre perpendicular TANTO à velocidade da carga quanto às linhas de Campo Magnético em que ela entra.

@card QT1wMjAuMGRqJg
Conexão entre matrizes ($A\mathbf{x}=\mathbf{b}$) e Circuitos Elétricos
@@
A Matriz $A$ é a representação do circuito. Na análise nodal típica, a diagonal principal é a soma das condutâncias conectadas ao nó, e os termos fora da diagonal são as condutâncias compartilhadas (negativas).
